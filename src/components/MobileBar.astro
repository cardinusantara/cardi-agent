<div id="mobileBar">
  <div class="mob-bar-info">
    <div class="price mono">Rp 26.946.000<span style="font-weight:500; color:var(--muted); font-size:11px;" data-i18n="checkout_unit">/bln</span></div>
    <div class="seats mono" data-i18n="mob_seats_left">6 slot tersisa</div>
  </div>
  <div class="mob-bar-actions">
    <a class="btn" href="https://api.whatsapp.com/send?phone=6281289895646&text=Halo%20Cardi%20Agent!%20Saya%20ingin%20jadwalkan%20demo%20untuk%20bisnis%20saya.%20%F0%9F%98%81" target="_blank" rel="noopener" data-i18n="nav_demo_btn">Jadwalkan Demo</a>
    <button type="button" class="mob-bar-close" id="mobBarClose" aria-label="Tutup Promo" onclick="window.closeMobileBar(event)">
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
        <line x1="18" y1="6" x2="6" y2="18"></line>
        <line x1="6" y1="6" x2="18" y2="18"></line>
      </svg>
    </button>
  </div>
</div>

<script is:inline>
  window.closeMobileBar = function(e) {
    if (e) e.stopPropagation();
    var bar = document.getElementById('mobileBar');
    if (bar) {
      bar.classList.remove('show');
      bar.classList.add('dismissed');
      try {
        sessionStorage.setItem('cardi_mob_bar_dismissed', '1');
      } catch (err) {}
    }
  };

  document.addEventListener('DOMContentLoaded', function() {
    var bar = document.getElementById('mobileBar');
    var hero = document.querySelector('.hero');
    if (!bar || !hero) return;
    try {
      if (sessionStorage.getItem('cardi_mob_bar_dismissed') === '1') {
        bar.classList.add('dismissed');
        return;
      }
    } catch (err) {}

    bar.classList.add('ready');
    var io = new IntersectionObserver(
      function(entries) {
        if (!bar.classList.contains('dismissed')) {
          bar.classList.toggle('show', !entries[0].isIntersecting);
        }
      },
      { threshold: 0 }
    );
    io.observe(hero);
  });
</script>
